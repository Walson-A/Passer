/**
 * The App Group shared by the app, its share extension and its Shortcuts actions.
 * The same identifier appears in `app.config.ts` (the entitlement) and in the
 * Swift code under `targets/`.
 */
export const APP_GROUP = 'group.direct.passer.app';
