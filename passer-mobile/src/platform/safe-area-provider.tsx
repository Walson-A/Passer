/**
 * The device's safe areas. On the web the library measures the page, which has
 * none, so the bench supplies the phone's instead (`safe-area-provider.web.tsx`).
 */
export { SafeAreaProvider } from 'react-native-safe-area-context';
