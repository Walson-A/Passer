/** Bench stand-in (see `web-bench.ts`): a browser has no Control Center. The bench opens widget links instead. */
export function takeInboxAction(): { action: string; at: number } | null {
  return null;
}
